# Index

Run from the repo root after you add or change source markdown:

```bash
make project-index PROJECT=<this-directory-name>
```

Add a `description:` field in each source file's YAML frontmatter so the table
stays useful without an LLM summarizer.
